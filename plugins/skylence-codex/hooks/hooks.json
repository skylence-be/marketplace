{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_ROOT=\"${CODEX_PLUGIN_ROOT:-}\"; if [ -z \"$PLUGIN_ROOT\" ] || [ ! -f \"$PLUGIN_ROOT/hooks/skyline-session-start.sh\" ]; then exit 0; fi; sh \"$PLUGIN_ROOT/hooks/skyline-session-start.sh\"",
            "statusMessage": "Loading Skylence Codex guidance"
          }
        ]
      }
    ]
  }
}
