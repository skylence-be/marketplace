{
  "description": "Judge hook (PreToolUse rules engine, opt-in via ~/.claude/judge-rules.json) + writing-guard on skyline_edit patches + SessionStart steer to prefer skyline MCP tools over built-in file tools.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "mcp__skyline.*__skyline_edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/writing-guard.sh\"",
            "statusMessage": "Checking writing guidelines..."
          }
        ]
      },
      {
        "matcher": "Bash|Write|Edit|NotebookEdit|mcp__",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/judge-hook.sh\"",
            "statusMessage": "Judging tool call..."
          }
        ]
      }
    ]
  }
}