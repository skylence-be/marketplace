{
  "_description": "Safe command matched by no rule is allowed (ls/cat/grep now have skyline-steering denies, so use echo)",
  "_expected_exit": 0,
  "tool_name": "Bash",
  "tool_input": {
    "command": "echo build finished"
  }
}
